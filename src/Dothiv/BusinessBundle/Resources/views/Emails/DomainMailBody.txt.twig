You can now claim your new .hiv domain using the token below.

 domain: {{ domain.name }}
 token: {{ domain.claimingToken }}
 link: {{ path('dothiv_charity_domain_claim', {'locale': 'en', 'token': domain.claimingToken }) }}

-- Your dotHIV team
